const header = document.querySelector('header');
const catalogSection = document.querySelector('.section');
let headerHeight = header.offsetHeight;
catalogSection.style.marginTop = headerHeight + 'px'; 